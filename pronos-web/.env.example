# URL de l'API backend (avec /api/v1 a la fin).
# En dev local, laissez VIDE : le proxy Vite redirige /api -> http://localhost:8080
# En production, mettez l'URL publique du backend, ex :
#   VITE_API_URL=https://pronos-backend.onrender.com/api/v1
VITE_API_URL=
