{
  "name": "pronos-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend d'analyse de paris sportifs : matchs reels, cotes, CLV, journal de decisions IA, tableau de bord pro.",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "worker": "JOBS_ONLY=true node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "stripe": "^16.8.0",
    "zod": "^3.23.8"
  }
}
